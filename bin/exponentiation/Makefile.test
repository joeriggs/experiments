
TARGET = test

OBJS = fp_exponent.o test.o

DEBUG ?= 0
ifeq ($(DEBUG), 1)
        DEBUG_FLAGS := -O0 -g -DDEBUG
else
        DEBUG_FLAGS := -Os
endif

%.o: %.c
	gcc -DTEST $(DEBUG_FLAGS) -Wall -Werror -c -o $@ $<

dummy: $(TARGET)

$(TARGET): $(OBJS)

clean:
	rm -f $(TARGET) $(OBJS)

